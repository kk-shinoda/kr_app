import 'package:flutter/material.dart';

class BankBookPage extends StatelessWidget {
  const BankBookPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('通帳')),
    );
  }
}
